# 发布砚台 · 角色书架 V1.4.3

用户安装入口只保留 GitHub 链接和酒馆助手 JSON。README 中不再提供单独 ZIP 安装方式。

## 本次发布内容

- 首页自动避让主题改写后的工具栏和浮动输入栏。
- 更新按钮的禁用状态使用不可用光标，仅真正处理版本操作时显示等待光标。
- GitHub 根目录、版本清单和助手 JSON 同步为 1.4.3。

## 维护者发布步骤

1. 构建正式文件并验证助手语法、交互以及 V1.4.2 更新器读取新版清单的兼容性。
2. 将本次 `releases` 中两个构建文件上传到 `main/releases/`，保留所有旧版文件。
3. 将根目录的正式文件上传到 `main`，包含新增的 `layout.js`、`manifest.json`、`versions.json`、脚本、样式和说明。
4. 等待 `Prepare bookshelf versions` 工作流通过，再核对固定分支 `v1.4.3` 与助手文件 SHA-256。

沿用现有的固定版本分支发布流程：`releases` 中的 ZIP 是该工作流的构建输入，用来冻结版本代码；它不再作为用户单独下载的安装方式。现有发布脚本与工作流无需修改。

## 发布后核对

- 根目录 `manifest.json` 和 `versions.js` 都是 1.4.3，`versions.json` 的 latest 是 1.4.3。
- 固定分支 `v1.4.3` 存在，其源码与本次正式文件一致。
- `v1.4.0`、`v1.4.1`、`v1.4.2` 的提交及原发布文件保持不变。
- 使用 V1.4.2 的用户可在书架“版本与更新”中检查、更新到 V1.4.3，然后刷新酒馆。
- V1.4.3 的上一兼容版本为 V1.4.2。

正式版与 V1.4.3 布局测试版版本号相同。测试用户需重新导入正式助手 JSON，或停用测试扩展，启用原 GitHub 版后更新。
